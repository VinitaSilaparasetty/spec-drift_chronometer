{
  "enabled": true,
  "name": "Conventional Commit Assistant",
  "description": "Generates professional commit messages based on staged diffs.",
  "version": "1.0.0",
  "when": {
    "type": "userTriggered"
  },
  "then": {
    "action": "askAgent",
    "prompt": "Analyze 'git diff --staged' and propose a Conventional Commit message."
  }
}
